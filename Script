// main.js — Mejora técnica del sitio Nonoalko

// Scroll suave entre secciones (mejorado con scrollIntoView + offset para navegadores móviles)
document.querySelectorAll('a[href^="#"]').forEach(enlace => {
  enlace.addEventListener('click', function (e) {
    e.preventDefault();
    const target = document.querySelector(this.getAttribute('href'));
    if (target) {
      target.scrollIntoView({
        behavior: 'smooth',
        block: 'start'
      });
    }
  });
});

// Animación de entrada cuando los elementos entran al viewport (Intersection Observer)
const animarEnScroll = document.querySelectorAll('section, .gallery img, form');
const observer = new IntersectionObserver(entradas => {
  entradas.forEach(entrada => {
    if (entrada.isIntersecting) {
      entrada.target.classList.add('visible');
    }
  });
}, {
  threshold: 0.2
});

animarEnScroll.forEach(el => observer.observe(el));

// Confirmación visual al enviar formulario
const form = document.querySelector('form');
form?.addEventListener('submit', () => {
  setTimeout(() => {
    alert('🎨 ¡Gracias por tu encargo! Te contactaremos pronto.');
  }, 300);
});

// Organización responsiva de galería
window.addEventListener('resize', ajustarGaleria);
document.addEventListener('DOMContentLoaded', ajustarGaleria);

function ajustarGaleria() {
  const columnas = window.innerWidth < 600 ? 1 : (window.innerWidth < 900 ? 2 : 3);
  const galeria = document.querySelector('.gallery');
  galeria.style.gridTemplateColumns = `repeat(${columnas}, 1fr)`;
}

// Alternador de tema (opcional)
// document.querySelector('#modo-oscuro')?.addEventListener('click', () => {
//   document.body.classList.toggle('modo-oscuro');
// });
